'use strict';

const chai = require('chai');
const should = chai.should();
const expect = chai.expect;

var Versionizr = require('../lib/index.js');

describe('#Versionizr Test Running...', () => {
    it('Should pass this test because it can\'t fail', () => {
        console.log('This test passed');
    });
});

